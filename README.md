# Patent FormuList

Fullstack веб-приложение для управления патентными формулами

## 📋 Описание

Patent FormuList — это современное веб-приложение, построенное на React с использованием Vite для frontend и FastAPI для backend. Проект включает систему аутентификации, регистрации пользователей и управления данными.

## 🚀 Технологии

### Frontend
- **React** 19.2 — библиотека для создания пользовательских интерфейсов
- **Vite** 6.0 — быстрый инструмент сборки и dev-сервер
- **React Router DOM** 7.9 — маршрутизация для SPA

### Backend
- **FastAPI** 0.115 — современный Python веб-фреймворк
- **Uvicorn** 0.34 — ASGI сервер
- **Pydantic** 2.10 — валидация данных

## 🔧 Установка

### Требования
- **Node.js** 18+ и npm
- **Python** 3.8+

### Установка всех зависимостей

Установить все зависимости одной командой
npm run install:all

### Установка по отдельности

**Frontend:**
npm install

**Backend:**
pip install -r backend/requirements.txt

## ▶️ Запуск проекта

### Запуск frontend + backend одновременно

npm start

После запуска откроются:
- **Frontend:** http://localhost:5173
- **Backend API:** http://localhost:8000
- **API Документация:** http://localhost:8000/docs

### Запуск по отдельности

**Только Frontend:**
npm run dev

**Только Backend:**
npm run backend

## 📝 Доступные команды

| Команда | Описание |
|---------|----------|
| `npm start` | Запускает frontend и backend одновременно |
| `npm run dev` | Запускает только Vite dev server |
| `npm run backend` | Запускает только FastAPI backend |
| `npm run build` | Создаёт production сборку frontend |
| `npm run preview` | Предпросмотр production сборки |
| `npm run install:all` | Устанавливает все зависимости (Node + Python) |
| `npm run clean` | Очищает кэш и папки сборки |

## 🌐 API Endpoints

### Аутентификация

- `POST /api/register` — Регистрация нового пользователя
{
"email": "user@example.com",
"password": "Password123"
}

- `POST /api/login` — Вход пользователя
{
"email": "user@example.com",
"password": "Password123"
}

## ⚙️ Конфигурация

### Vite Proxy

В `vite.config.js` настроен прокси для API запросов:[web:24][web:25]

proxy: {
'/api': {
target: 'http://localhost:8000',
changeOrigin: true,
}
}

Это позволяет делать запросы с frontend к backend без CORS проблем в разработке.

### CORS на Backend

FastAPI настроен для приёма запросов с Vite dev-сервера:

allow_origins=[
"http://localhost:5173",
"http://127.0.0.1:5173",
]

## 🛠 Разработка

### Добавление новых зависимостей

**Frontend (npm):**
npm install название-пакета

**Backend (pip):**
pip install название-пакета
pip freeze | grep название-пакета >> backend/requirements.txt

### Hot Reload

Оба сервера (Vite и FastAPI) поддерживают горячую перезагрузку:
- Изменения в React компонентах применяются мгновенно
- Изменения в Python коде перезапускают FastAPI автоматически
